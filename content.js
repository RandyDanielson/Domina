// Content script for Tab Grouper extension
// This script runs on all web pages but has minimal functionality
// The main tab grouping logic is handled by the background service worker

(function() {
  'use strict';
  
  // Simple content script that could be extended for future features
  // Currently just logs that the extension is active on the page
  
  console.log('Tab Grouper extension is active on this page');
  
  // Future enhancements could include:
  // - Page-specific grouping rules
  // - Visual indicators for grouped tabs
  // - Custom domain detection logic
  // - Integration with page content for smarter grouping
  
})();
